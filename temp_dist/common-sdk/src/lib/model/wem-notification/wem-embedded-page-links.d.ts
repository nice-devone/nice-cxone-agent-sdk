import { WemEmbeddedPage } from './wem-embedded-page';
/**
 * model interface for WEM embedded page links
 */
export interface WemEmbeddedPageLinks {
    /**
    * @remarks list of wem embedded pages
    */
    links: Array<WemEmbeddedPage>;
}
