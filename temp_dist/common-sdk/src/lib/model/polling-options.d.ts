export interface PollingOptions {
    /**
     * @remarks polling flag to identify if polling is needed or not
     */
    isPolling: boolean;
    /**
     * @remarks Polling interval in milliseconds
     */
    pollingInterval: number;
}
