/**
 * Enum for http request header custom types to be passed in API calls
 */
export declare const enum HttpRequestCustomHeaders {
    /**
     * The server should read this custom header to know sender application name
     */
    X_MESSAGE_SENDER = "x-message-sender"
}
