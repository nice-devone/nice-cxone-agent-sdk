/**
 * Util to get feature toggle enablement
 * @param featureName - feature toggle Name
 * @returns - boolean
 * @example -
 * ```
 * isVoiceTranscriptionEnabledAndToggledOn();
 * ```
 */
export declare const isVoiceTranscriptEnabledAndToggledOn: () => Promise<boolean>;
