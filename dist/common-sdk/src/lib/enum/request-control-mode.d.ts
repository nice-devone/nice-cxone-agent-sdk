/**
 * Enum for request control mode to determine whether to abort or delay the request when there is a request
 */
export declare enum RequestControlMode {
    ONLY_ABORT = "ONLY_ABORT",
    ONLY_DELAY = "ONLY_DELAY",
    DELAY_AND_ABORT = "DELAY_AND_ABORT"
}
