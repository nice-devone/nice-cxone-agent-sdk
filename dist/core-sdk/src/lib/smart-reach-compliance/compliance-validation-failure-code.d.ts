/**
 * Enum representing the validation failure code for a compliance validation event.
 */
export declare enum ComplianceValidationFailureCode {
    CUSTOMER_NOT_FOUND = 1,
    ZIPCODE_MISSING = 2
}
