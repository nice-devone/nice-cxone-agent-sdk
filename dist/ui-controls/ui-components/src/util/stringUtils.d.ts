/**
 * function to capitilize first letter of string.
 * @returns String in first letter capital
 * @example
 * ```
 * capitalizeFirstLetter("string")
 * ```
 */
export declare const capitalizeFirstLetter: (str: string) => string;
/**
 * function to return the expected length of a phone number
 * @returns phone number length
 * @example
 * ```
 * getPhoneNumberFormatSize()
 * ```
 */
export declare const getPhoneNumberFormatSize: () => number;
/**
 * function to format a phone number
 * @returns (###) ###-####
 * @example
 * ```
 * formatPhoneNumber("string")
 * ```
 */
export declare const formatPhoneNumber: (inputNumber: string) => string;
/**
 * function to compare two strings
 * @returns true/false
 * @example stringCompareIgnoreCase(a,b)
 */
export declare const stringCompareIgnoreCase: (variable1: string | undefined, variable2: string | undefined) => boolean;
