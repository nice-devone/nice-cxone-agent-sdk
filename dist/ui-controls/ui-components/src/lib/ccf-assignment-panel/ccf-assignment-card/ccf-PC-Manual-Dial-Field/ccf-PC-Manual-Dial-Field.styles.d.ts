import { Theme } from '@mui/material';
/**
 * CcfCustomerCard - Is used to display the channels information of customer under customer card.
 * @param props -?-theme
 * @example <customerCardChannelStyles />
 */
declare const pcManualDialFieldStyles: (theme: Theme) => {
    manualPCInnerTextField: {
        padding: string;
        backgroundColor: string;
    };
    manualPCOuterTextField: {
        margin: string;
    };
};
export default pcManualDialFieldStyles;
