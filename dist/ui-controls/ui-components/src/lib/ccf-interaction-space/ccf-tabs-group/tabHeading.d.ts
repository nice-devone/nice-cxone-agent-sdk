/// <reference types="react" />
export interface TabHeadingProps {
    children: React.ReactNode;
    isSelected: boolean;
    isPreviewCase: boolean;
}
/**
 * @returns
 * @example -
 */
export declare const TabHeading: ({ children, isSelected, isPreviewCase }: TabHeadingProps) => JSX.Element;
