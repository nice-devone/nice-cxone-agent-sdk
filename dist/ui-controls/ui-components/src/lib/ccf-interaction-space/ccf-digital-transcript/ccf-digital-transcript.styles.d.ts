import { Theme } from '@mui/material';
/**
 * Component displays text input to send the transcript
 * @returns Component to display Transcript Section
 * ```
 * @example
 * <CcfDigitalTranscript/>
 * ```
 */
declare const CcfDigitalTranscriptStyles: (theme: Theme) => {
    zeroPadding: {
        padding: number;
    };
    dialogWrapper: {
        width: string;
        maxWidth: string;
        padding: string;
        height: string;
    };
    dialogWrapperSm: {
        width: string;
        maxWidth: string;
        padding: string;
        height: string;
    };
    flexCenter: {
        display: string;
        justifyContent: string;
        alignItems: string;
    };
    flexPadding: {
        display: string;
        padding: string;
    };
    emailAddressText: {
        fontWeight: string;
        lineHeight: string;
        textTransform: string;
    };
    closeIcon: {
        position: string;
        right: number;
        top: number;
    };
};
export default CcfDigitalTranscriptStyles;
