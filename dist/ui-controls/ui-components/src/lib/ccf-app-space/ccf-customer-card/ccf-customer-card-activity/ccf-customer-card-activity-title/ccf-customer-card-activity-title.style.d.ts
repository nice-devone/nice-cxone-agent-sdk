import { Theme } from '@mui/material';
/**
 * CcfCustomerCard - used to display quick replies component
 * @param props -?-customerCardDetailsStyles
 * @example <customerCardDetailsStyles />
 */
declare const customerCardActivityTitleStyles: (theme: Theme) => {
    smallFont: {
        fontSize: string;
        textDecoration: string;
    };
    title: {
        fontSize: string;
        fontWeight: string;
    };
    iconCRMType: {
        backgroundImage: string;
        backgroundRepeat: string;
        backgroundSize: string;
        backgroundPosition: string;
        border: string;
        padding: string;
        borderRadius: string;
        maxWidth: string;
        '&:hover': {
            background: string;
        };
    };
};
export default customerCardActivityTitleStyles;
