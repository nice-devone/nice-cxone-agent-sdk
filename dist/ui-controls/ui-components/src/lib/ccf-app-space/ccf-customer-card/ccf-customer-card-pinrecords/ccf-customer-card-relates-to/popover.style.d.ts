import { Theme } from '@mui/material';
/**
 * Styles for the Relates To popover.
 * @example styles()
 */
declare const styles: (position: any, isRelatesToPopoverOpen: boolean, shouldOverflow: boolean, theme: Theme, iconBase64string?: string | undefined) => string;
export default styles;
