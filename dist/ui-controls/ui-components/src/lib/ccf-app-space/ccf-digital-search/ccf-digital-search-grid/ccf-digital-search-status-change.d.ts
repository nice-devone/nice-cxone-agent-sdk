export interface CcfInteractionSearchStatusList {
    id: string;
    name: string;
}
/**
 * Component to show the Select Options status menu for the interaction search on row selection
 * @example
 * ```
 * <CcfDigitalSearchStatusChange />
 * ```
 */
export declare const CcfDigitalSearchStatusChange: () => JSX.Element;
