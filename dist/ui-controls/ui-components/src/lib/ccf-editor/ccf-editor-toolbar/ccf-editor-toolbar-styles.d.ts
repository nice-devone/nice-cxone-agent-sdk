import { Theme } from '@mui/material';
/**
 * Styling for ccf-editor-toolbar
 * @returns ccf-editor-toolbar CSS properties as a JSON object
 * @example ccfEditorToolbarStyles(theme)
 */
export declare const ccfEditorToolbarStyles: (theme: Theme) => {
    buttonActive: {
        background: string | undefined;
        marginRight: string;
    };
    toolbarContainer: {
        backgroundColor: string;
        width: string;
        border: string;
        boxShadow: string;
        borderRadius: string;
        padding: string;
        display: string;
        flexDirection: string;
        flexWrap: string;
        gap: string;
    };
};
export default ccfEditorToolbarStyles;
