/**
 * Modal for Agent State History Report
 */
export class BulkReplyHistoryResponse {
    /**
    * Function to parse the response from API to model
    * @param data - Data object received
    * @example -
    * ```
    * parse(data);
    * ```
    */
    parse(data) {
        this.bulkReplyHistory = data;
    }
    ;
}
//# sourceMappingURL=bulk-reply-history.js.map