/**
 * Enum representing different filter types.
 */
export declare enum CopilotFilterTypes {
    /**
    * Represents standard filter operation
    */
    FILTER = "Filter",
    /**
    * Represents an append operation which indicates it will be sent with rephrased question
    */
    APPEND = "Append"
}
